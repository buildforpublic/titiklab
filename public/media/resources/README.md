# Resources

Place downloadable files here. The Resources page currently links to:

- `pamphlet.pdf` — the Tagungguk learning pamphlet (in progress)

Add the file with the exact name above, or update the link in `app/resources/page.tsx`.
